# MobileNetV2 aggregate package

This package is only intended to install all its deps at once.

```
$ ck clean env --tags=tensorflowmodel,python,mobilenet-v2 -f
$ ck install package:tensorflowmodel-mobilenet-v2
```